server:
  port: 0 # ??? ?? ??

spring:
  application:
    name: user-service

eureka:
  instance:
    # 'hostname:???' ?? eureka instance id ??(???? ??? 'hostname:application.name:server.port' ? id. - ?? 0 ???? id? ???)
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eurika: true
    fetch-registry: true # eureka ????? ?????? ??? ????? ???
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

